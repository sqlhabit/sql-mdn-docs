mysql:
  min_version:
  related_pages:
    - name: date_trunc() function in MySQL
      url: /mdn/date-trunc-in-mysql
postgresql:
  min_version: 0
sqlite:
  min_version:
  related_pages:
    - name: date_trunc() function in SQLite
      url: /mdn/date-trunc-in-sqlite
redshift:
  min_version: 0
big_query:
  min_version: 0
  partial_support: true
  related_pages:
    - name: date_trunc() function in BigQuery
      url: /mdn/date-trunc-in-bigquery
snowflake:
  min_version: 0
